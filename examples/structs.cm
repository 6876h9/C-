struct Point {
    x: i32,
    y: i32,
}

struct Color {
    r: u8,
    g: u8,
    b: u8,
}

fn create_point(px: i32, py: i32) -> void {
    let point = Point { .x = px, .y = py };
    let x_coord = point.x;
}

fn create_color() -> void {
    let red = Color { .r = 255, .g = 0, .b = 0 };
}
