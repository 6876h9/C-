fn pointer_basics() -> void {
    let x: i32 = 42;
    let ptr: *i32 = &x;
    let value = *ptr;
}

fn pointer_array() -> void {
    let arr: [i32; 10];
}

extern fn malloc(size: u64) -> *void;

fn allocate() -> void {
    let block: *void = malloc(100);
}
